global:
  domain: argocd.dataplumbers.ch
server:
  # Disable SSL to allow to configure TLS connection on the ingress
  insecure: false
  service:
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
  ingress:
      enabled: true
      controller: generic
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
        nginx.ingress.kubernetes.io/ssl-redirect: "true"
        nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
      ingressClassName: "nginx"
      hostname: "argocd.dataplumbers.ch"
      tls: 
        - hosts:
            - "argocd.dataplumbers.ch"
          secretName: argocd-server-tls
      path: /
  
configs:
  rbac:
    policy.csv: |
      g, jan.matter@dataplumbers.ch, role:admin
    policy.default: role:readonly
  cm:
    dex.config: |
      connectors:
        - type: oidc
          id: infomaniak
          name: Infomaniak
          config:
            issuer: https://login.infomaniak.com
            clientID: $infomaniak-argocd-oauth-credentials:clientID
            clientSecret: $infomaniak-argocd-oauth-credentials:secret
            redirectURI: https://argocd.dataplumbers.ch/api/dex/callback
            scopes:
              - openid
              - profile
              - email
          insecureSkipVerify: false
          

